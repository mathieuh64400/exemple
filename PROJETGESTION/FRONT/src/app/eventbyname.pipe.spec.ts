import { EventbynamePipe } from './eventbyname.pipe';

describe('EventbynamePipe', () => {
  it('create an instance', () => {
    const pipe = new EventbynamePipe();
    expect(pipe).toBeTruthy();
  });
});
