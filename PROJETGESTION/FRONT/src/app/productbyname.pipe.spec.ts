import { ProductbynamePipe } from './productbyname.pipe';

describe('ProductbynamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductbynamePipe();
    expect(pipe).toBeTruthy();
  });
});
