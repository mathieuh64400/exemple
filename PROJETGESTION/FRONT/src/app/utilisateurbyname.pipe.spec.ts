import { UtilisateurbynamePipe } from './utilisateurbyname.pipe';

describe('UtilisateurbynamePipe', () => {
  it('create an instance', () => {
    const pipe = new UtilisateurbynamePipe();
    expect(pipe).toBeTruthy();
  });
});
